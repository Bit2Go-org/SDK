{
  "name": "@bit2go/web",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "homepage": "https://github.com/Bit2Go-org/SDK",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "run-s build:*",
    "build:module": "tsup index.ts --clean --out-dir lib --dts --format esm,cjs --target esnext --metafile",
    "build:global": "tsup index.ts --out-dir lib --dts --format iife --global-name bit2goWeb --minify --target esnext --platform browser --metafile"
  },
  "dependencies": {
    "@bit2go/api": "workspace:*"
  }
}
